version: 2
jobs:
  build:
    working_directory: /usr/src/semver
    docker:
      - image: nimlang/nim
    branches:
      only:
        - master
    steps:
      - checkout
      - run:
          name: test
          command: nimble test
  build_alpine:
    working_directory: /usr/src/semver
    docker:
      - image: nimlang/nim:alpine
    branches:
      only:
        - master
    steps:
      - checkout
      - run:
          name: test
          command: nimble test
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - build_dev
      - build_alpine
